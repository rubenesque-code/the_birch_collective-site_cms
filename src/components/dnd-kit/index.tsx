import { DndSortableContext } from "./DndSortableContext";
import { DndSortableElement } from "./DndSortableElement";

export const DndKit = () => {
  throw new Error(
    "DndKit exists for naming purposes only and should not be used as a component",
  );
};

DndKit.Context = DndSortableContext;
DndKit.Element = DndSortableElement;
