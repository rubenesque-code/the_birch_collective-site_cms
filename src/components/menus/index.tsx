import { Button } from "./Button";

function Menu() {
  throw new Error(
    "Menu exists for naming purposes only and should not be used as a component",
  );
}

export { Menu };

Menu.Button = Button;
