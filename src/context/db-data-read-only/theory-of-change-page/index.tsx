import { Section } from "./section";

function TheoryOfChangePage() {
  throw new Error(
    "TheoryOfChangePage exists for naming purposes only and should not be used as a component",
  );
}

export { TheoryOfChangePage };

TheoryOfChangePage.Section = Section;
