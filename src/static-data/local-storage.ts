const auth_persistence_key = "auth-persistence-key";
const email_signin_key = "email-signin-key";

const keys = {
  auth_persistence: auth_persistence_key,
  email_signin: email_signin_key,
};

export const localStorage = {
  keys,
};
