export type Header = {
  aboutUs: {
    popover: {
      heading: string;
      subheading: string;
    };
  };
  getInvolved: {
    popover: {
      heading: string;
      subheading: string;
    };
  };
};
